{
    "name": "banker",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "api:dev": "yarn workspace api dev",
        "api:start": "yarn workspace api start",
        "api:build": "yarn workspace api build",
        "web:dev": "yarn workspace web dev",
        "web:start": "yarn workspace web start",
        "web:build": "yarn workspace web build",
        "lint": "yarn lint:source && yarn lint:config && yarn lint:spell",
        "lint:fix": "eslint './packages/**/*' --fix --quiet",
        "lint:source": "eslint './packages/**/*' --quiet --cache --cache-strategy='content' --cache-location='.eslintcache/'",
        "lint:config": "prettier --loglevel warn --check {*,**/*}.{json,yml,yaml}",
        "lint:spell": "cspell lint '**' --no-summary --no-progress",
        "lint:clean": "rm -rf ./eslintcache && rm .cspellcache"
    },
    "dependencies": {
        "dayjs": "^1.11.2",
        "uuid": "^8.3.2"
    },
    "devDependencies": {
        "@rimac-technology/eslint-config": "^25.0.0",
        "@types/node": "16.11.38",
        "@types/uuid": "^8.3.4",
        "cspell": "^6.1.1",
        "eslint": "8.17.0",
        "prettier": "^2.6.2",
        "typescript": "4.7.3"
    },
    "packageManager": "yarn@3.2.0",
    "workspaces": [
        "packages/*"
    ]
}
